# 📊 Data

- `raw/` — untouched, original data. Never edit these files directly.
- `processed/` — cleaned/transformed data, ideally produced by a script in `analysis/` so it's reproducible.

⚠️ **Large files (>50–100MB):** don't commit them here. Add to `.gitignore` and store on an institutional drive, OSF, or Zenodo instead — link to it below.

**External data locations:**
- [link + description]
